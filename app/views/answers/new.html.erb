<h1>Post a new answer!</h1>
<%= form_for [@question, @answer] do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :body %>
  <%= f.text_area :body %>
  <%= f.submit "Add Answer" %>
<% end %>
<br />
<%= link_to 'Return Home', questions_path %>
